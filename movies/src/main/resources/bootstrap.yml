spring:
  application:
    name: movies-service
  cloud:
    config:
      discovery:
        service-id: config-server
        enabled: true
      fail-fast: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
      
server:
  port: 8081